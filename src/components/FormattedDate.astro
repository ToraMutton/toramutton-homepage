---
// 日付表示。Blog一覧・トップページと同じ "2026/09/05" 形式に揃える
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('ja-JP', {
			year: 'numeric',
			month: '2-digit',
			day: '2-digit',
		})
	}
</time>
